# ===== 云端 OCR 代理（发布到 Cloudflare Pages 后生效） =====
# 生产环境一般无需填写：默认走同域 /api/ocr（Pages Functions 代理，密钥在服务端）。
# 仅当需要把前端指向其它域名/Worker 时，填写完整 URL，例如：
# VITE_OCR_WORKER_URL=https://ocr-proxy.你的用户名.workers.dev

# 换模型 / 换 OpenAI 兼容服务商（本地调试很常用）：
# 下面三项会随请求体传给 /api/ocr，优先级高于服务端环境变量。
# ⚠️ 设置了 VITE_OCR_ENDPOINT 后，服务端密钥将发送到该地址，请务必同时配置
#    ACCESS_TOKEN（与服务端一致），且只填你信任的接口地址。
# VITE_OCR_ENDPOINT=https://openrouter.ai/api/v1/chat/completions
# VITE_OCR_MODEL=openai/gpt-4o-mini
# VITE_OCR_FALLBACK_MODEL=anthropic/claude-3-5-sonnet

# 可选：与服务端环境变量 ACCESS_TOKEN 对应，前端随请求发送（弱口令防刷）
# VITE_ACCESS_TOKEN=your-secret-token

# ===== 高德地图（Web 端 JS API） =====
# 注意：这些 Key 会进入前端 JS，必须在高德控制台配置「域名白名单」只允许你的站点域名！
# VITE_AMAP_KEY=xxxx
# VITE_AMAP_SECURITY_CODE=xxxx

# ===== 服务端环境变量（在 Cloudflare Pages 控制台配置，不进前端） =====
# OPENROUTER_API_KEY      必填，OpenRouter 密钥（云函数 functions/api/ocr.js 读取）
# OCR_MODEL               可选，主识别模型名（带供应商前缀，如 openai/gpt-4o-mini），默认 gpt-4o-mini
# OCR_FALLBACK_MODEL      可选，备选模型名；当主模型 429/503/504 时自动切换重试一次
# OPENROUTER_ENDPOINT     可选，OpenRouter 接口域名，默认 https://openrouter.ai/api/v1/chat/completions
# ACCESS_TOKEN            可选，弱口令防刷，需与上方 VITE_ACCESS_TOKEN 保持一致
